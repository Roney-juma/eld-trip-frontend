import React from 'react';
import { Typography, Container } from '@mui/material';

const LogSheet = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Daily Log Sheets</Typography>
      {/* Add canvas here */}
    </Container>
  );
};

export default LogSheet;